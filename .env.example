# Environment Configuration

# Set to "production" for strict CSP and security headers
# Leave unset or "development" for dev mode with unsafe-eval allowed
NODE_ENV=development

# Server port (default 8080)
PORT=8080

# Server host (default localhost)
HOST=localhost

# Log level: debug, info, warn, error
LOG_LEVEL=debug

# Use bundled vendor files (dist/vendor.bundle.js) instead of individual vendor files
# Only applicable in production
USE_BUNDLED=false
